!+
!
! Procedure CFICI
!
! This procedure take in the transformation coefficients from 
! XYFIT and stores them as the input to CINVERT so the inverse
! transformation coefficients can be calculated.
!
$ LET CINVERT_TRCOEFFS=_XYFIT_TRCOEFFS
$ EXIT
