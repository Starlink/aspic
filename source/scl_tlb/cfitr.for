!+
!
! This procedure takes in the transformation coefficients from
! XYFIT and stores them as the input to XYTRAN so we can
! calculated RA and Dec directly from X and Y.
!
$ LET XYTRAN_TRCOEFFS=_XYFIT_TRCOEFFS
$ EXIT
